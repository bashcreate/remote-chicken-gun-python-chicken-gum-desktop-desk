# Пример: при генерации pipreqs список будет содержать реальные зависимости по импорту.
# Ниже — рекомендованные утилиты для разработки и проверки (не всегда нужны в production):
flake8
pytest
pipreqs
mypy
black
